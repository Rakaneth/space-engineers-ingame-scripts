R e a d m e
-----------

Cargo containers to be restocked should be configured with [Restock] and item keys,
using the same keys as the AutoAssembler script.

Cargo containers to be used as sources should be configured with [RestockSource].

Run the script with no argument.
